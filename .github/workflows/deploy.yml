name: Deployment
on: push

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      -  run: echo 'run tests'

  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - run: deploy 'production deployment'
